default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV["DB_POOL"] || ENV['MAX_THREADS'] || 5 %>
  host: <%= ENV["POSTGRES_HOST"] || "localhost" %>
  username: <%= ENV["POSTGRES_USER"] || "residential_sales" %>
  password: <%= ENV["POSTGRES_PASSWORD"] || "residential_sales" %>

development:
  <<: *default
  database: maryland_residential_sales_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: maryland_residential_sales_test

production:
  <<: *default
  database: maryland_residential_sales_production